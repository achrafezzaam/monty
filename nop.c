#include "monty.h"
/**
 * nop - Add the top two elements of the stack.
 * @stack: The stack element
 * @line_number: The command line number
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;
}
